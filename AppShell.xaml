<?xml version="1.0" encoding="utf-8" ?>
<Shell
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:SaveUpp.View"
    x:Class="SaveUpp.AppShell">

    <TabBar>
        <ShellContent Title="Home" ContentTemplate="{DataTemplate local:MainPage}" />
        <ShellContent Title="Neues Produkt" ContentTemplate="{DataTemplate local:NewProductPage}" />
        <ShellContent Title="Gespeicherte Produkte" ContentTemplate="{DataTemplate local:SavedProductsPage}" />
    </TabBar>
</Shell>
